- if seat.user && seat.user == current_user
  This is your current seat.
- elsif seat.user && current_user.requested?(seat.object)
  = link_to 'Cancel request', seat_request_path(current_user.request(seat)), class: 'btn btn-primary', method: :delete, remote: true
- elsif seat.user
  = link_to 'Request this seat', seat_seat_requests_path(seat), class: 'btn btn-primary', method: :post, remote: true
- else
  = link_to pick_seat_path(seat), class: 'btn btn-primary', method: :post, remote: true do
    Pick this seat
